version: '3.8'

services:
  music-pro:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3004:3000"
    env_file:
      - .env
    environment:
      - NODE_ENV=production
    restart: always
